<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xdweb.first.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="org.xdweb.first.model.User">
        <id column="userId" jdbcType="INTEGER" property="userid" />
        <result column="userName" jdbcType="VARCHAR" property="username" />
        <result column="userPassword" jdbcType="VARCHAR" property="userpassword" />
        <result column="isAdmin" jdbcType="TINYINT" property="isadmin" />
    </resultMap>
    <sql id="Base_Column_List">
        userId, userName, userPassword, isAdmin
    </sql>

    <select id="selectByUsernameAndPasswordAndIsAdmin" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user
        where userName = #{username}
        and userPassword = #{password}
        and isAdmin = #{isAdmin}
        limit 1
    </select>
</mapper>
